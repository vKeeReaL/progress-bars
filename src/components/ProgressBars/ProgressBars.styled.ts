import styled from "@emotion/styled";

const StyledProgressBars = styled.section`
  width: 100%;
  max-width: 320px;
  h1 {
    font-size: 32px;
    font-weight: 600;
    padding: 1rem 0;
  }
`;

const StyledProgressActionsPanel = styled.div`
  display: flex;
  gap: 10px;
`;

export { StyledProgressBars, StyledProgressActionsPanel };
